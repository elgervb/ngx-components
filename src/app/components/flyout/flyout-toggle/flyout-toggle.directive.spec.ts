import { FlyoutToggleDirective } from './flyout-toggle.directive';

describe('FlyoutToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new FlyoutToggleDirective();
    expect(directive).toBeTruthy();
  });
});
